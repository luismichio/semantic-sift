# Core dependencies (matches pyproject.toml [project.dependencies])
mcp>=1.0
numpy>=1.24
opentelemetry-api>=1.20
opentelemetry-sdk>=1.20
psutil>=5.9

# Optional extras — install via: uv pip install .[neural,multi-modal]
# neural: llmlingua, torch, transformers, sentence-transformers
# multi-modal: markitdown>=0.1
