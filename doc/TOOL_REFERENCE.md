# Semantic-Sift: FastMCP Tool Reference

This document provides the exhaustive operator's manual for all FastMCP tools exposed by the Semantic-Sift server (`server.py`). It details the specific arguments, default values, routing logic, and expected outputs for each tool.

---

## 1. File Analysis & Reading

### `sift_read_file`
**Intent**: Reads a local file directly from disk and applies the appropriate distillation pipeline before returning the output. This is the primary ingestion tool designed to bypass standard IDE file-reading limitations and prevent context flooding.
*   **Arguments**:
    *   `path` (str): The absolute or relative path to the local file to read.
    *   `rate` (float, default: `0.5`): The compression target used when the Semantic Engine is engaged. Lower is more aggressive.
    *   `type` (str, default: `"auto"`): The specific sifter pipeline to force. Options are `"logs"`, `"chat"`, `"doc"`, `"extraction"`, or `"auto"`.
*   **Routing & Multi-Modal Logic**:
    *   **Binary Files**: If the extension is `.pdf`, `.docx`, `.xlsx`, `.pptx`, `.zip`, or `.html`, the tool automatically uses **MarkItDown** to convert the content to Markdown.
    *   **Two-Stage Cache**: Raw Markdown conversions are stored in `.sift_cache/raw_[hash].md` to ensure subsequent sifts (at different rates) are near-instant.
    *   **Auto-Detection** (for `type="auto"`):
        *   `.log`, `.out` routes to the **Heuristic Engine** (`sift_kernel.apply_heuristic_sieve`).
        *   `.md`, `.txt`, `.rst`, and converted binary files route to the **Hybrid Pipeline** (`sift_kernel.perform_doc_sift`).
        *   All other extensions (including code files) route to the **Semantic Engine** (`sift_kernel.perform_semantic_sift`).
*   **Output**: The distilled file content, prepended with a Markdown Audit Header (generated by `telemetry_core.generate_audit_header`) detailing the format, token reduction, and latency. 

### `sift_analyze_file`
**Intent**: Acts as a "radar" to estimate the noise ratio of a local file without polluting the context window with the file's actual content.
*   **Arguments**:
    *   `path` (str): The absolute or relative path to the file to analyze.
*   **Logic**:
    *   Loads the file and calculates the density of timestamps (ISO-8601, legacy, bracketed), UUIDs, and structural repetition (e.g., `===`, `...`).
    *   `Noise Ratio = ((timestamps * 10) + (uuids * 5) + (repetition * 2)) / char_count * 100` (capped at 100%).
*   **Output**: A Markdown report containing the file's character count, estimated noise ratio, and an actionable recommendation:
    *   Noise > 15.0%: Recommends `sift_read_file(type='logs')`.
    *   Length > 8000: Recommends `sift_read_file(type='doc' or 'chat')`.
    *   Length < 1000: Recommends native reading (no sifting required).
    *   Otherwise: Recommends optional sifting.

---

## 2. Content Distillation

### `sift_logs`
**Intent**: Sifts through raw technical logs to remove repetitive structural noise while preserving critical errors and stack traces.
*   **Arguments**:
    *   `raw_text` (str): The verbose log or terminal output string to be cleaned.
*   **Logic**: Routes directly to `sift_kernel.apply_heuristic_sieve`.
*   **Output**: The cleaned text string.

### `sift_chat`
**Intent**: Performs intelligent BERT-based semantic compression on natural language prose and conversation history.
*   **Arguments**:
    *   `text` (str): The prose or chat history string to be semantically condensed.
    *   `rate` (float, default: `0.5`): The compression target. `0.3` = Aggressive (save 70%), `0.5` = Balanced, `0.7` = Gentle (save 30%).
*   **Logic**: Routes directly to `sift_kernel.perform_semantic_sift` which utilizes the `llmlingua` SLM.
*   **Output**: The compressed text string.

### `sift_doc`
**Intent**: Performs a hybrid two-stage distillation on long documentation files.
*   **Arguments**:
    *   `text` (str): The raw content of the documentation file.
*   **Logic**: Routes directly to `sift_kernel.perform_doc_sift`. It applies the heuristic sieve to remove structural formatting (footers/headers/page numbers) and then applies semantic compression at a hardcoded rate of `0.4`.
*   **Output**: The distilled document string.

### `sift_extraction`
**Intent**: Refines raw extractions from OCR or PDF parsing tools (like Docling or LiteParse).
*   **Arguments**:
    *   `content` (str): The raw string output from an extraction tool.
*   **Logic**: Routes directly to `sift_kernel.perform_extraction_cleaning`. Applies specific regex patterns to strip metadata debris (e.g., copyright notices, page counts) and then protects Markdown structure by applying semantic compression at a gentler hardcoded rate of `0.7`.
*   **Output**: The refined Markdown string.

---

## 3. Ranking & Utilities

### `sift_rank`
**Intent**: Ranks a list of text chunks by semantic relevance to a user query using a local BGE-Reranker. Ensures the context window only processes the highest-value signal.
*   **Arguments**:
    *   `query` (str): The user's prompt or search query.
    *   `documents` (list[str]): An array of text strings (chunks) to be ranked.
    *   `top_n` (int, default: `3`): The maximum number of top-ranked results to return.
*   **Logic**: Routes directly to `sift_kernel.perform_ranking` which utilizes `sentence_transformers.CrossEncoder('BAAI/bge-reranker-base')`.
*   **Output**: A formatted Markdown string containing the Top-N results, their confidence scores, and truncated content previews (up to 500 characters per result).

### `sift_analyze`
**Intent**: Provides a technical context health report (Signal-to-Noise Ratio) for an arbitrary string of text, similar to `sift_analyze_file` but for memory-resident data.
*   **Arguments**:
    *   `text` (str): The string to be analyzed.
*   **Logic**:
    *   Checks for explicit host truncation markers (e.g., `<tool_output_masked>` or `Output too large.`). If found, returns an immediate "MANDATORY SIFT raw file" recommendation.
    *   Otherwise, calculates the identical Noise Ratio formula used in `sift_analyze_file`.
*   **Output**: A Markdown report recommending the appropriate distillation tool (`sift_logs`, `sift_doc`, `sift_chat`, or none).

---

## 4. System Operations

### `sift_onboard`
**Intent**: Automatically configures the current project workspace for optimal context sifting by injecting instructions, security hooks, and shielding specialized subagent threads. It also ensures critical Semantic-Sift artifacts are protected from version control exposure.
*   **Arguments**:
    *   `target_dir` (str, default: `None`): Optional absolute path to the project root. Defaults to the current working directory.
*   **Logic**:
    *   **Recursive Subagent Discovery**: Scans for specialized agent folders (`.codex/agents/`, `.cursor/agents/`, `.junie/agents/`) and scoped `AGENTS.md` files in subdirectories.
    *   **Multi-Format Shielding**: Injects the Sift Mandate into Markdown, rule files, and **TOML** configurations (for Codex CLI agents).
    *   **Automated `.gitignore` Protection**: Detects existing `.gitignore` files and automatically appends rules for `.sift_identity`, `.sift_telemetry.json`, and `.sift_cache/` if not already present.
    *   **Rule Auditing**: Scans files for contradictory instructions (e.g., "always use view_file") and overrides them.
    *   **Automated Hook Injection**: 
        *   Merges `sift_hook.py` into `.cursor/hooks.json`, `.github/hooks/semantic-sift.json`, `~/.claude/settings.json`, `~/.qwen/settings.json`, and `~/.codex/settings.json`.
        *   Injects **Security Gateways** into `.windsurf/hooks.json` and `.clinerules/hooks/PreToolUse.ps1` to block native file readers > 1KB.
        *   Generates native TypeScript plugins for OpenCode and OpenClaw.
*   **Output**: A Markdown "Onboarding Report" detailing the environment (Python, CUDA, Device), security status (including Git protection), a list of **Shielded Subagents**, and a bulleted manifest of all modified files and injected hooks.

### `get_sift_stats`
**Intent**: Retrieves performance metrics and token savings data from the local telemetry registry (`.sift_telemetry.json`).
*   **Arguments**:
    *   `scope` (str, default: `"current"`): Use `"current"` for the active session (filtered by `SESSION_ID`) or `"all"` for historical totals across all sessions.
*   **Logic**:
    *   Reads `.sift_telemetry.json`.
    *   Aggregates total tool calls, original tokens processed, final tokens, cache hits, and average latency.
    *   Provides a per-tool breakdown of savings.
    *   Respects the `SIFT_TELEMETRY_DISABLED` kill-switch.
*   **Output**: A formatted string detailing the client identity, tier, aggregate metrics, and the specific per-tool breakdown of tokens saved and cache hits.